package cl.empresa.depositocpc.enums;

/**
 * Roles del sistema. En el MVP todos los usuarios autenticados tienen el mismo
 * acceso; la autorización diferenciada por rol se implementará en una fase posterior.
 */
public enum RolUsuario {
    OPERADOR,
    ADMIN
}
